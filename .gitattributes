# registry.yaml is generated by scripts/build-registry.sh — never hand-merge it.
# - linguist-generated: collapse it in GitHub diffs and exclude from language stats.
# - merge=regenerate-registry: on a merge conflict, regenerate it instead of textual merge.
#   Enable the driver once per clone: run `bash scripts/setup.sh` (it's a no-op to re-run).
registry.yaml linguist-generated=true merge=regenerate-registry
