**When writing docs, or restating guidance that already exists elsewhere** → `agent-repo-layout`
- Pointers, not copies — one document owns each piece of reasoning; everything else links to it. When a
  pointer and its source disagree, the source wins and the pointer is stale.
