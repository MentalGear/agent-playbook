**When you hit the second instance of the same defect shape** → `stuck-on-a-problem`
- Stop fixing one at a time: enumerate the class, fix every instance together, land a checked guard.
- Repeated same-class fixes mean you're working below the right layer, not that the next patch is the one.
